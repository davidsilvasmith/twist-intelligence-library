# TWiST Intelligence Library

V1.0 Live.

A structured intelligence system for This Week in Startups.

## Core idea

Each episode becomes a **Masterclass**.
Each recurring founder becomes a **Living Trajectory**.
The dashboard reads from a central `wiki/dashboard_data.json` file and renders:
- hero episodes
- founder delta cards
- relationship constellation
- anti-portfolio misses
- tactical masterclass sidebars

## What is live in V1.0

- Audited hero set for **E2271-E2273**
- Tactical framework slot for **E2269**
- Founder trajectory pages
- Episode masterclass pages
- D3.js venture constellation
- Rigor Mode toggle for high-signal filtering
- Anti-portfolio hindsight layer
- Production deploy at `http://twist.forge.dsdoes.com`

## D3.js graph schema

The venture constellation is driven by `wiki/dashboard_data.json`.

Each node includes:
- `label`
- `type`
- `company`
- `connected_to`
- `url`
- `delta_tooltip`
- `active`

### Node groups

- `guest` → founder / operator
- `company` → startup entity
- `vc` → venture node
- `concept` → abstract market or ecosystem layer
- `host` → Jason / TWiST anchor

### Link model

The build script converts each node's `connected_to` array into a deduplicated force-graph edge list.
Every source and target must have an explicit node entry. That is the schema rule used to avoid orphan graph edges.

## Karpathy Compiler architecture

The pipeline has four layers:

1. **Discovery**
   - pull latest episode list from the TWiST YouTube channel feed
   - store ordered episode metadata in `wiki/raw/`

2. **Compilation**
   - one episode at a time
   - Gemini CLI or fallback research produces compressed structured outputs
   - write raw output to `wiki/raw/E####_*.json`
   - write human-readable masterclass files to `wiki/masterclasses/`

3. **Relational hardening**
   - founder pages in `wiki/founders/`
   - company pages in `wiki/companies/`
   - trajectory and anti-portfolio logic verified through external reporting

4. **Presentation sync**
   - Stitch UI export analyzed into a schema map: `wiki/ui_schema_map.json`
   - dashboard hydrated through `wiki/dashboard_data.json`
   - static pages generated by `scripts/build_twist_site.py`

## Important files

- `wiki/dashboard_data.json`
- `wiki/ui_schema_map.json`
- `wiki/masterclasses/`
- `wiki/raw/`
- `scripts/build_twist_site.py`
- `scripts/finalize_twist_data.py`
- `scripts/twist_episode_worker.sh`

## Current status

The library is live and demo-ready.
The current hardened showcase is:
- E2269 tactical framework
- E2271 Boardy / Barksdale pivot
- E2272 workflow economics
- E2273 Project Glasswing / inference moat
